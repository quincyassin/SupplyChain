spring:
  datasource:
    url: jdbc:mysql://mysql:3306/order_split_merge?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: root
    password: ${MYSQL_ROOT_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update

app:
  cors:
    allowed-origins: http://localhost,http://127.0.0.1
